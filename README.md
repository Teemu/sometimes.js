# sometimes.js
Sometimes returns true, sometimes false

## Install

````
npm install sometimes
````

## Usage

````javascript
var sometimes = require('sometimes');
if (sometimes()) {
  // do something
} else {
  // do something else
}
````
